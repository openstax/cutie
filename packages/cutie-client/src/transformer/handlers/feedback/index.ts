/**
 * Feedback handler registration module
 * Import all feedback handlers to trigger side-effect registration with the registry
 */
import './feedbackBlock';
import './feedbackInline';
import './modalFeedback';
