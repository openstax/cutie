/**
 * Represents a feedback identifier option for the dropdown
 */
export interface FeedbackIdentifierOption {
  id: string;
  label: string;
  description: string;
  interactionType: string;
  responseIdentifier: string;
}
